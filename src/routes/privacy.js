import { h } from 'preact'
import Splash from '../components/splash'

export default () => (
  <Splash>
    <p>
      &yet will never to gather, store, or sell information about you, log your
      messages or gifs, or engage in any other behavior that would compromise
      your privacy and security in any way. Facecamp is a static web application
      with no database, so your gifs and messages are uploaded directly to Slack
      with no data passing through our servers.
    </p>
  </Splash>
)
